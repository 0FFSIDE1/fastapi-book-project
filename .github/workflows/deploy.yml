name: Deployment Pipeline

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy to Server
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Deploy Application
        run: |
          ssh OFFSDIE@SHA256:rNcW31Y4gBv4NMe0aXdDQufLCknTnafcBofjcn0lYho "cd fastapi-book-project/main.py && git pull origin main && sudo systemctl restart main.service"
